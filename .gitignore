.env
web/.env
web/.env.*
!web/.env.example

web/node_modules/
web/package-lock.json
web/dist/

# IDE / local tooling
.cursor/
*.log
.DS_Store
tmp/

# docker-compose.prod.yml Postgres bind mount (default ./data/postgres)
data/postgres/

# Keep go.sum versioned. Embed needs at least one file under dist/ when not running `make build-web`:
internal/webembed/dist/*
!internal/webembed/dist/placeholder.txt
*.exe
booklab
